import { TradingDashboard } from '@/components/dashboard/TradingDashboard';

/**
 * Dashboard Page
 * 
 * Main trading dashboard page with real-time data and controls.
 * Implements the complete trading interface with portfolio overview,
 * trading controls, and live market data.
 */

export default function Dashboard() {
  return <TradingDashboard />;
} 